name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  placeholder:
    name: Placeholder CI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder
        run: echo "Real build, lint, and test steps will be added later."
